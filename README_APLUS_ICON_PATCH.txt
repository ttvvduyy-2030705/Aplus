Aplus icon patch only.

Copy/merge these files into the root of your Aplus-main project, replacing existing files.
This patch only changes app icon/image assets; it does not change app code or features.

After replacing, clean/rebuild Android and uninstall the old app from device/emulator if launcher still shows cached old icon.
